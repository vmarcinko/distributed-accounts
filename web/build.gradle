apply plugin: 'war'

archivesBaseName = 'distributed-accounts-web'

dependencies {

    compile (
            "org.springframework:spring-webmvc:4.0.2.RELEASE",
            "org.springframework:spring-context-support:4.0.2.RELEASE",
			"org.freemarker:freemarker:2.3.20"
    )
}
